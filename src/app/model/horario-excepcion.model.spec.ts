import { HorarioExcepcion } from './horario-excepcion.model';

describe('HorarioExcepcion', () => {
  it('should create an instance', () => {
    expect(new HorarioExcepcion()).toBeTruthy();
  });
});
